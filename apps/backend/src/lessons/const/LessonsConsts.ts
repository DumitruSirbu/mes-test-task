/**
 * Route path segment for the lessons controller.
 */
export const LESSONS_ROUTE_PREFIX = 'lessons';

/**
 * Route path segment for the "my courses" endpoint.
 */
export const ME_COURSES_ROUTE = 'me/courses';

/**
 * Route path segment for listing lessons within a course.
 */
export const COURSE_LESSONS_ROUTE = 'courses/:id/lessons';

/**
 * Route path segment for fetching a single lesson by UUID.
 */
export const LESSON_BY_ID_ROUTE = 'lessons/:id';
